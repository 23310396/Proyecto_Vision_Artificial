# Evidencias

Aquí se guardarán las imágenes generadas por `src/predict.py` con las detecciones del modelo.

Después de entrenar y copiar `best.pt` a la carpeta `models/`, ejecuta:

```bash
python src/predict.py
```

El resultado se guardará en:

```text
evidencias/detecciones/
```
