# Modelos

Después del entrenamiento, copia aquí el archivo `best.pt` generado por YOLOv8.

Normalmente queda en una ruta parecida a:

```text
runs/train/logos_automotrices_yolov8/weights/best.pt
```

Cópialo como:

```text
models/best.pt
```
